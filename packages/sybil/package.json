{
  "name": "@probatio/sybil",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Making a track record expensive to fake.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@probatio/pools": "*" }
}
