{
  "name": "@probatio/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "An MCP server so agents can read and verify a Probatio record, on proof rather than promises.",
  "bin": { "probatio-mcp": "./dist/server.js" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@probatio/sdk": "*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  }
}
