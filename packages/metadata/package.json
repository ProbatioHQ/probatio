{
  "name": "@probatio/metadata",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Token names, symbols and images. On-chain decoding plus a guarded fetch of attacker-controlled off-chain JSON.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@probatio/pools": "*",
    "bs58": "^6.0.0"
  }
}
