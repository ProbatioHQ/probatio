{
  "name": "@probatio/seasons",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Season rulesets, their lifecycle, and how a pot is split.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@noble/hashes": "^1.6.0", "@probatio/sim": "*" }
}
