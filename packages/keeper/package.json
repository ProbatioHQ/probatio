{
  "name": "@probatio/keeper",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Batches trades and commits them on chain, crash-safely.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@probatio/commit": "*",
    "@probatio/db": "*",
    "@probatio/sim": "*",
    "@probatio/pools": "*",
    "@probatio/payments": "*",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.6.0",
    "bs58": "^6.0.0"
  }
}
