{
  "name": "@probatio/profile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Display names, and the rules that stop one being used to impersonate.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": {}
}
