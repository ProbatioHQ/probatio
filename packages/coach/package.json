{
  "name": "@probatio/coach",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Turning a trader's own numbers into advice, without letting a model invent any of them.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@probatio/analytics": "*" }
}
