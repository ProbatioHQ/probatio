{
  "name": "@probatio/sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fill engine. Pure functions, no I/O, no network. Deterministic and independently testable.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
