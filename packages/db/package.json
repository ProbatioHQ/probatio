{
  "name": "@probatio/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Schema and data access. Storage only \u2014 all arithmetic happens in @probatio/sim.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15"
  },
  "devDependencies": {
    "@probatio/analytics": "*",
    "@probatio/seasons": "*",
    "@probatio/scoring": "*"
  }
}
