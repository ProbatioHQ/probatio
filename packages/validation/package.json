{
  "name": "@probatio/validation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Replays real historical swaps through the fill engine and measures the error. The gate everything else stands on.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@probatio/pools": "*",
    "@probatio/sim": "*"
  }
}
