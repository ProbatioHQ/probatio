{
  "name": "@probatio/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read and independently verify a Probatio trading record against the chain.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@probatio/commit": "*",
    "@probatio/pools": "*",
    "bs58": "^6.0.0"
  }
}
